{
  "dependencies": {
    "fast-async": "^6.2.2",
    "node-fetch": "^1.6.3"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-env": "^1.1.8"
  },
  "scripts": {
    "build": "babel --copy-files src --out-dir build",
    "watch": "babel --copy-files --watch src --out-dir build"
  }
}
